// JavaScript Document
// The jQuery's .ready method checks if the page is ready for your code to work with
$(document). ready(function () {
// here we that if the button with #burger in on touch ul should display 
$("#burger").on("touch",funtion {
				$("#myDropdown").show();
	

	
	
});
				
				
			